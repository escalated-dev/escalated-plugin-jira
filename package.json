{
  "name": "@escalated-dev/plugin-jira",
  "version": "0.1.0",
  "description": "Jira issue linking, status sync, and auto-create for Escalated tickets — TypeScript SDK version",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@escalated-dev/plugin-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
